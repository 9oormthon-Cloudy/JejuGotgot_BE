
spring:
  jpa:
    hibernate:
      ddl-auto: create

  application:
    name: api
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/krampoline

springdoc:
  swagger-ui:
    path: /api/swagger-ui/index.html
